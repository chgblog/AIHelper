<Application x:Class="AIHelper.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:AIHelper">
    <Application.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="AppForeground" Color="#e0e0e0"/>
        <SolidColorBrush x:Key="AppAccent" Color="#7c3aed"/>
        <SolidColorBrush x:Key="AppBorder" Color="#3a3a5e"/>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3a3a5e"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="CaretBrush" Value="#e0e0e0"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#2a2a3e"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#3a3a5e"/>
            <Setter Property="Padding" Value="10,5"/>
        </Style>
    </Application.Resources>
</Application>
